name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: printing greeting
        run: echo "heloo world"
      - name: print goodbye
        run: echo "done - bye"
